# Nano4 GP1 生醫 HPC 課程規劃（講師版）

> 對象：授課講師與助教
> 計畫：`GOV115088`（國網生技醫藥高效能運算推廣與應用計畫），CPU-only，所有 Slurm 作業使用 `ngs62g`，每個作業固定 `-c 8 --mem=62G`
> 線上講義：<https://gemini960114.github.io/Nano4-Docs/>

## 1. 課程總覽

| 場次 | 章節 | 時間 | 學員結束時能做到 |
| :--- | :--- | :---: | :--- |
| **第一堂（基礎）** | 第 01 章 登入、2FA 與環境管理<br>第 02 章 VS Code Remote-SSH 與 AI 工具鏈 | 3 小時 | 用 VS Code / Antigravity 連上 Nano4（透過 ssh-proxy 只認證一次），分清楚 `$HOME` 與 `/work`，送出第一個 Slurm 作業 |
| **第二堂（基礎）** | 第 03 章 Slurm 語法與作業調度<br>第 04 章 AI 輔助生醫質控管線 | 3 小時 | 寫出並送出標準、陣列、相依作業；用 `seff` 檢查資源；在登入節點跑 FastQC / MultiQC 並在瀏覽器看報告 |
| **進階課** | 第 05 章 AI Agent 自動化 Slurm 排程<br>第 06 章 AI Agent 技能總匯庫<br>第 07 章 nf-core/ampliseq 真實 16S 案例 | 3 小時 | 讓 AI Agent 把流程改寫成 Slurm 作業、安裝 Skills，並完成一次 nf-core/ampliseq 官方測試 |

設計原則：

- 第一堂只做環境與連線。初學者最容易卡在 2FA、VS Code 連線與 ssh-proxy，保留足夠的除錯時間。
- 第二堂以第 04 章「登入節點的限制 → 為什麼需要 Slurm」收尾，自然銜接進階課。
- 第 07 章的 pipeline 需要長時間執行（官方測試約 30 分鐘以上），獨立成進階課，並在課程一開始就送出作業。

---

## 2. 課前準備

### 2.1 學員（課前一週寄出，第一堂開始前完成）

| # | 項目 | 參考 | 檢核方式 |
| :---: | :--- | :--- | :--- |
| 1 | 註冊 iService 帳號，並由講師加入計畫 `GOV115088` | 第 01 章 §2 | iService 計畫成員清單中有自己 |
| 2 | 手機安裝 IDExpert App 並完成 2FA 綁定 | 第 01 章 §2 | 能收到推播或產生 OTP |
| 3 | 用終端機 `ssh 帳號@nano4.nchc.org.tw` **登入一次**（系統才會建立 `$HOME`） | 第 01 章 §3 | 看到 `[帳號@25a-lgn0X ~]$` 提示字元 |
| 4 | 安裝 VS Code 或 Google Antigravity | 第 02 章 §2 | 能開啟程式 |
| 5 | 下載 ssh-proxy 執行檔（Windows / macOS Apple Silicon / Linux） | 第 02 章 §2 步驟 E | 檔案已存在電腦中 |
| 6 | （選做）申請 Taiwan AI RAP API Token | 第 02 章 §6 | 取得 Token |

### 2.2 講師 / 助教

| 時間點 | 項目 | 說明 |
| :--- | :--- | :--- |
| 課前一週 | 確認所有學員已加入 `GOV115088` | 用學員帳號執行 `sbatch --test-only -A GOV115088 -p ngs62g -c 8 --mem=62G --wrap true`，出現 `to start at` 即代表帳號與分區可用 |
| 課前一週 | 確認計畫錢包額度 | `wallet GOV115088` |
| 課前一天 | 講師帳號完整跑過當堂所有練習 | 講義中的 `sbatch` 範例都已於 2026-09-23 實測通過 |
| 課前一天 | 確認 `ngs62g` 空閒狀況 | `sinfo -p ngs62g -o "%D %C"`，第二個欄位為「已用/空閒/其他/總數」核心 |
| 進階課前 | 確認第 07 章 `config/nano4.config` 的 `queueSize = 5`（見 §6；已於 2026-09-23 調整） | 避免整班同時送出大量子作業；學員人數差很多時再依 §6 調整 |
| 每堂課 | 準備一台可投影的電腦示範 Windows 與 macOS 兩種 ssh-proxy 啟動方式 | 第一堂最常見的問題 |

---

## 3. 第一堂：連線與環境（第 01–02 章，3 小時）

| 時間 | 單元 | 講義位置 | 學員實作 | 講師重點 / 常見狀況 |
| :---: | :--- | :--- | :--- | :--- |
| 0:00–0:15 | 開場、確認課前準備 | 課程總綱 | 舉手確認課前作業 1–5 完成情況 | 未完成 2FA 綁定者請助教一對一協助 |
| 0:15–0:30 | Nano4 與 GP1 架構：登入節點、計算節點、DTN、`/work` | 第 01 章 §1、課程總綱「硬體與網路架構」 | — | 強調 GP1 生醫節點是 Nano4 的一部分；本課程只用 `ngs62g` |
| 0:30–0:50 | SSH 登入與 2FA、設定 `~/.ssh/config` | 第 01 章 §3、§4 | 用 `ssh nano4` 登入；`git clone` 課程教材到 `$HOME/Nano4-Docs` | Windows 需確認已安裝 OpenSSH Client；推播沒收到時改選 OTP |
| 0:50–1:10 | 儲存空間與模組：`$HOME` vs `/work`、`hfsquota`、`module` | 第 01 章 §7、§8 | **練習 1** 環境健檢（`quick_healthcheck.sh`） | 大型資料一律放 `/work/$USER`；計算節點沒有 Java，要載入 `biology/JDK` |
| 1:10–1:25 | 第一個 Slurm 作業 | 第 01 章 §12 | **練習 5** `sbatch` 第一個 CPU 作業（8 核心 / 62G） | 只預覽，細節留到第二堂；確認每人都能成功送出 |
| 1:25–1:35 | 休息 | | | 助教處理前面卡住的學員 |
| 1:35–2:05 | VS Code / Antigravity Remote-SSH 連線 | 第 02 章 §2 步驟 A–D | 連上 `nano4`、Open Folder 開啟 `/work/<帳號>` | 此時學員會體驗到「每開一次資料夾就要再認證一次」，作為下一段的動機 |
| 2:05–2:35 | **ssh-proxy：只認證一次** | 第 02 章 §2 步驟 E | 設定 `nano4-proxy`、啟動 proxy、改連 `nano4-proxy` | 最容易出錯的一段。確認 `User` 已改成自己的帳號、proxy 視窗保持開啟；建議啟動時加 `--max-lifetime 10h` |
| 2:35–2:50 | 擴充套件與 `AGENTS.md` 治理守則 | 第 02 章 §3、§8 | **練習 1** 開啟遠端工作區 | 說明 AI Agent 會讀取 `AGENTS.md`，所以產生的 Slurm 腳本會自動符合 `ngs62g` 規格 |
| 2:50–3:00 | 總結、預告第二堂 | | 下課前在 proxy 視窗按 `Ctrl+C` | 提醒：共用電腦務必停止 proxy |

**時間不夠時可略過（改為課後自學）**：第 01 章 §5 DTN 傳檔、§6 T3 搬遷、§9 Apptainer、§10 `uv`、練習 2–4、練習 6；第 02 章 §4–§7 AI 工具與 Taiwan AI RAP 設定、練習 2–4。

**有多的時間可加入**：第 01 章 **練習 6** `salloc` 互動節點；第 02 章 **練習 2** Gemini 與 Codex 對話比較。

**第一堂結束檢核點**：每位學員都能 (1) 透過 `nano4-proxy` 用 VS Code / Antigravity 開啟 `/work/<帳號>`，(2) 執行 `squeue --me` 看到或查得到自己送出過的作業。

---

## 4. 第二堂：Slurm 與生醫質控（第 03–04 章，3 小時）

| 時間 | 單元 | 講義位置 | 學員實作 | 講師重點 / 常見狀況 |
| :---: | :--- | :--- | :--- | :--- |
| 0:00–0:15 | 複習、重新連線 | 第 02 章 §2 步驟 E | 啟動 ssh-proxy、連上 `nano4-proxy` | 先確認全班都連上再開始 |
| 0:15–0:35 | 為什麼需要 Slurm、`ngs62g` 官方規格 | 第 03 章 §1、§2、§3「本課程標準範例」 | **Lab 1** `wallet`、`sacctmgr` 查詢帳號與分區 | 官方規定每個佇列固定「核心 × 記憶體」搭配，`ngs62g` 一律 `-c 8 --mem=62G`，不可自行縮減 |
| 0:35–1:00 | `#SBATCH` 參數與資源四要素 | 第 03 章 §3、§4 | **Lab 2** 提交標準 CPU 作業 | 日誌用 `%x-%j.out`；不要寫死不存在的 `logs/` 目錄 |
| 1:00–1:25 | 陣列作業與相依作業 | 第 03 章 §5 A–B | **Lab 4** 陣列作業（`1-10%4`）、**Lab 5** 三段相依流水線 | 作業約 17 秒就跑完，`squeue` 不一定看得到；改用 `sacct -j <JOB_ID> -X -o JobID,Start,End` 看開始時間分成三批（4、4、2），即可看出 `%4` 的效果。不要用 `watch squeue` 反覆刷新 |
| 1:25–1:35 | 休息 | | | |
| 1:35–1:55 | 互動節點與效能分析 | 第 03 章 §5 D、§6 | **Lab 6** `salloc`、**Lab 7** `seff` | `salloc` 用完一定要 `exit` 釋放資源；說明 exit 137 = 記憶體不足 |
| 1:55–2:10 | FASTQ、FastQC、MultiQC 概念 | 第 04 章 §1 | — | 非生醫背景學員：強調這是「資料 → 工具 → 報告」的通用模式 |
| 2:10–2:35 | 在登入節點執行小型質控 | 第 04 章 §2–§4 | 下載 4 個樣本、執行 `run_fastqc_multiqc.sh` | 資料只有 4 × 1000 reads，屬登入節點允許的小型測試；確認已載入 JDK 模組 |
| 2:35–2:50 | 在瀏覽器檢視 MultiQC 報告 | 第 04 章 §5 | VS Code 連接埠轉送開啟報告 | 結束後執行 `tmux kill-session -t svc-multiqc-report` |
| 2:50–3:00 | 登入節點的限制、預告進階課 | 第 04 章 §6 | — | 「資料變大就必須用 Slurm」→ 銜接第 05 章 |

**時間不夠時可略過**：第 03 章 §5 C/E GPU 與跨節點內容、§7 容器、Lab 3（本來就跳過）、Lab 7；第 04 章 §2 prompt 技巧（直接使用提供的腳本）。

**第二堂結束檢核點**：每位學員都能 (1) 送出一個 `ngs62g` 作業並用 `seff` 看到 8 核心 / 62 GB 的配置，(2) 在瀏覽器打開自己的 MultiQC 報告。

---

## 5. 進階課：AI 自動化與真實案例（第 05–07 章，3 小時）

> 關鍵安排：**開課後 20 分鐘內就送出第 07 章的下載與官方測試**，讓 pipeline 在背景執行，講師同時講授第 05–06 章。

| 時間 | 單元 | 講義位置 | 學員實作 | 講師重點 / 常見狀況 |
| :---: | :--- | :--- | :--- | :--- |
| 0:00–0:10 | 複習、重新連線 | | 啟動 ssh-proxy | |
| 0:10–0:25 | **先送出第 07 章作業** | 第 07 章 §2.2、§3.2、§3.3 | 執行先備檢查 → 送出 `01_prepare_real_data.slurm`（約 5 分鐘）→ 送出 `02_run_official_test.slurm`（約 30 分鐘以上） | 兩個作業都用 `NFCORE_ACCOUNT=GOV115088`、`NFCORE_PARTITION=ngs62g`；送出後就不用管它 |
| 0:25–0:35 | 第 07 章故事：一位研究生的一天 | 第 07 章 §0 | — | 用故事說明為什麼先跑 test、pipeline 做了哪些步驟；§0.6 教學員看懂 `squeue` 畫面 |
| 0:35–1:05 | 讓 AI Agent 把第 04 章流程改寫成 Slurm | 第 05 章 §1–§4 | **Case A** 離線運算模式 | 比較 AI 產生的腳本與講義提供的腳本：`module purge`、JDK 模組、8/62G 是否都有 |
| 1:05–1:15 | 外網直連模式 | 第 05 章 §5 | **Case B**（時間不夠改為示範） | Nano4 計算節點可直接連外網，不需要 proxy |
| 1:15–1:25 | 休息 | | 執行一次 `squeue --me` 看第 07 章進度 | |
| 1:25–1:45 | 安裝 Skills | 第 06 章 | 執行 `sync_skills.sh`、`slurm-preflight.sh` | Claude Code 讀 `~/.claude/skills`，其他工具讀 `~/.agents/skills`，腳本會同時安裝到兩處 |
| 1:45–2:00 | 檢查官方測試結果 | 第 07 章 §3.3 | 查看 log 最後的 `Pipeline completed successfully` | 若仍在跑，先講 §1 資料來源 |
| 2:00–2:35 | 解讀結果 | 第 07 章 §5 | 用官方測試的輸出練習看 MultiQC、分類與多樣性結果 | 真實資料（§3.4）整體需 45 分鐘以上，課堂上由講師展示事先跑好的結果 |
| 2:35–2:50 | AI 重做與 Skill 封裝（示範） | 第 07 章 §4、§6 | 講師示範 | 學員課後自行完成，依 §7 繳交 |
| 2:50–3:00 | 總結、說明繳交內容 | 第 07 章 §7 | — | |

**時間不夠時可略過**：第 05 章 Case B（改示範）；第 07 章 §3.4 真實資料（改由講師展示）、§4、§6（改為課後作業）。

**進階課結束檢核點**：每位學員都能 (1) 說明 AI 產生的 Slurm 腳本與講義版本的差異，(2) 看到自己的 nf-core/ampliseq 官方測試顯示 `Pipeline completed successfully`。

---

## 6. 資源容量與排隊注意事項

`ngs62g` 共有 13 個節點（`25a-cpn[01-10,16-18]`）、1,664 個核心。以 2026-09-23 的空閒量（約 1,476 核心）估算，整個分區大約可同時執行 **184 個** 8 核心作業；每位使用者最多 120 個作業。

| 練習 | 每位學員同時作業數 | 30 位學員合計 | 評估 |
| :--- | :---: | :---: | :--- |
| 第 01 章練習 5、第 03 章 Lab 2 | 1 | 30 | 沒問題 |
| 第 03 章 Lab 4 陣列作業（`%4`） | 4 | 120 | 沒問題 |
| 第 03 章 Lab 6 `salloc` | 1 | 30 | 沒問題；提醒用完 `exit` |
| 第 05 章 Case A / B | 1 | 30 | 沒問題 |
| 第 07 章官方測試（原設定 `queueSize = 10`） | 1 + 10 | 約 330 | **超出容量，會大量排隊** |
| 第 07 章官方測試（目前設定 `queueSize = 5`） | 1 + 5 | 約 180 | 接近上限但可接受 |

建議：

- `07-nfcore-ampliseq-case-study/config/nano4.config` 的 `executor.queueSize` 已由 10 調為 5。若學員明顯多於 30 人，可再調低（例如 3），但官方測試會跑得更久。
- 第 07 章 §3.4 真實資料不要全班同時執行，改由講師事先跑好並展示結果。
- 實際學員人數若與 30 人差距較大，請依上表重新估算。

---

## 7. 實測時間參考（講師帳號，2026-09-23）

| 項目 | 實測時間 | 備註 |
| :--- | :---: | :--- |
| 第 01 章練習 5、第 03 章 Lab 2 | 約 5 秒 | 當天沒有排隊，送出後 1 秒內開始執行 |
| 第 03 章 Lab 4 陣列（10 個任務，`%4`） | 約 17 秒 | 分三批執行，可觀察到同時最多 4 個 |
| 第 03 章 Lab 5 三段相依流水線 | 約 17 秒 | 三段依序執行 |
| 第 05 章 Case A / Case B | 約 10 秒 | 產生真實 MultiQC 報告 |
| 第 07 章 §3.2 下載資料 | 約 5 分鐘 | ENA 偶爾中途斷線，腳本會自動重試續傳 |
| 第 07 章 §3.3 官方測試 | 約 27 分鐘 | `queueSize = 10` 時實測；125 個子作業，0 個失敗。調為 5 後的時間待補 |
| 第 07 章 §3.4 真實資料 | 45 分鐘以上 | 最久的步驟是 QIIME 2 稀釋曲線（alpha rarefaction）；確切時間待補 |
